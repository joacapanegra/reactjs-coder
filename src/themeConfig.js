import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#d87d4a",
    },
    secondary: {
      main: "#fbaf85",
    },
  },
});
